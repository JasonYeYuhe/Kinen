请直接在这个仓库里动手修改代码，不要只做分析。

你当前工作的项目根目录是：
`/Users/jason/Documents/Kinen`

先阅读这份交接文档：
`/Users/jason/Documents/Kinen/docs/PROJECT_AUDIT_HANDOFF.md`

你的任务：

1. 根据 handoff 文档，优先修复其中列出的高优先级问题。
2. 不要回滚或覆盖仓库里与本任务无关的现有本地改动。
3. 先做真正会影响质量和发布风险的问题，不要优先做表面优化。
4. 修改完成后，实际运行文档里要求的验证命令，而不是只说“理论上可行”。
5. 如果某一项无法安全修复，明确说明阻塞原因，并继续完成其余项。

本次优先目标，按顺序执行：

1. 修复 `KinenTests` target 配置，让以下命令通过：
   `xcodebuild test -scheme Kinen -destination 'platform=macOS'`
2. 修复 AI 默认开关与 UI 默认值不一致的问题，确保新用户不需要先进入设置页就能得到预期的 AI 自动分析行为。
3. 修复模板日记条目的编辑回写逻辑，确保已有模板条目进入编辑后不会丢内容、不会因为 `templateResponses` 未初始化而保存失败。
4. 完成或收窄 “Backup & Restore”：
   - 如果你能安全完成恢复流程，就补齐 restore UI 和数据恢复完整性
   - 如果你不能在本轮安全做完，就把 UI 文案改成不误导用户，只保留真实能力
5. 修复最核心的本地化接线问题：
   - 让现有 `Localizable.strings` 真正被代码使用
   - 优先处理最核心的用户界面，不要求一次性清空全仓所有硬编码字符串
6. 清理最明显的产品文案冲突，尤其是 iCloud sync / zero cloud / no cloud sync 之间的矛盾。

执行要求：

- 直接改代码，不要输出长篇计划后停住。
- 尽量小步、正确、可验证。
- 不要顺手做大规模重构，除非它是修复上述问题的必要条件。
- 不要为了“看起来完整”引入未经验证的大改动。

修改完成后请按下面格式汇报：

1. 已修复问题
2. 关键改动点
3. 实际运行过的验证命令及结果
4. 尚未解决的问题和剩余风险
